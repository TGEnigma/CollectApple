<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:viewmodels="clr-namespace:CollectApple.ViewModels" 
             x:DataType="viewmodels:RegisterViewModel"
             mc:Ignorable="d"
             x:Class="CollectApple.Views.RegisterPage">
    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="Center">
            <Entry Text="{Binding Email}" Placeholder="Email"/>
            <Entry Text="{Binding Password}" Placeholder="Password" IsPassword="True"/>
            <Entry Text="{Binding PasswordCheck}" Placeholder="Password validation" IsPassword="True"/>
            <Button VerticalOptions="Center" Text="Sign Up" Command="{Binding RegisterCommand}"/>
            <Label Text="Already have an account?"/>
            <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>